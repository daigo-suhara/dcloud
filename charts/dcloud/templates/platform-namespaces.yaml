{{- /*
knative-operator does not create knative-serving on its own; declaring
it here keeps the sync-wave ordering deterministic. Kourier's own
namespace ships inside manifests/kourier.yaml.
*/ -}}
{{- if .Values.knative.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: knative-serving
{{- end }}
